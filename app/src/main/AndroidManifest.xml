<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.redesaplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pruebas1"
        tools:replace="android:theme">
        <activity android:name=".OrderStatus"></activity>
        <activity android:name=".Canasta" />
        <activity android:name=".DescripcionComida"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan"/>
        <activity android:name=".menu_comidas" />
        <activity
            android:name=".menu_inicio"
            android:label="@string/title_activity_menu_inicio"
            android:theme="@style/Theme.Pruebas1.NoActionBar" />
        <activity android:name=".Registrar"/>
        <activity android:name=".Acceder"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>